# @oxvelte/cli-darwin-arm64

Platform-specific binary for [oxvelte](https://github.com/tolgaouz/oxvelte) (macOS ARM64 (Apple Silicon)).

This package is installed automatically as an optional dependency of `oxvelte`.
You should not need to install it directly.
