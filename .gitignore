# Build
/target
Cargo.lock

# Vendor (cloned by prepare.sh)
/vendor/

# Test fixtures (committed to main for CI)

# Runtime artifacts
results.tsv
*.log
run.log
build.log
test.log

# NOTE: .fixtures.sha256 is NOT ignored — it is the integrity checksum file
# and must be committed so the pre-commit hook can verify fixtures.

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# npm platform binaries (staged by scripts/build-npm.sh)
npm/@oxvelte/*/oxvelte
npm/@oxvelte/*/oxvelte.exe
npm/oxvelte/README.md

# OS
.DS_Store
Thumbs.db
/testbeds/
