# Build
/target
Cargo.lock

# Vendor (cloned by prepare.sh)
/vendor/

# Test fixtures copied from vendor (regenerated by prepare.sh)
/fixtures/

# Runtime artifacts
results.tsv
*.log
run.log
build.log
test.log

# NOTE: .fixtures.sha256 is NOT ignored — it is the integrity checksum file
# and must be committed so the pre-commit hook can verify fixtures.

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
/testbeds/
